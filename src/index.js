import React from 'react'; //import the librrary
import ReactDOM from 'react-dom';
import App from './App'; //import the main App component

ReactDOM.render(<App />, document.getElementById('root')); //renders the App component into that div element with id="root"
